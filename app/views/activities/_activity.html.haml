%tr
  %td= activity.name
  %td= activity.title
  %td= activity.begin_date
  %td= activity.ending
  %td= activity.details.count
  %td= activity._type
  %td= link_to "Show", activity
  %td= link_to "Details", activity_details_path(activity)
  %td= link_to "Edit", edit_activity_path(activity)
  %td= link_to "Destroy", activity, method: :delete, data: { confirm: 'Are you sure?' }
